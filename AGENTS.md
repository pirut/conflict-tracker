# Repository Working Defaults

- For implementation requests that modify code, complete the work end-to-end: run checks, then commit and push unless the user explicitly asks not to.
- Never commit plaintext secrets. Keep `.env.example` as placeholder-only values.
- Prioritize data quality over volume for conflict-monitoring ingestion.
